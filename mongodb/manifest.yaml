apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: roboshop
  name: mongodb
  labels:
    project: roboshop
    component: mongodb
    tier: db
spec:
  replicas: 1
  selector:
    matchlabels:
      project: roboshop
      component: mongodb
      tier: db
  template:
    metadata:
      name: mongodb
      labels:
        project: roboshop
        component: mongodb
        tier: db
    spec:
      cotainers:
        - name: mongodb
          image: santu268devops/mongodb:4.0.0
---
apiVersion: v1
kind: Service
metadata:
  namespace: roboshop
  name: mongodb
  labels:
    project: roboshop
    component: mongodb
    tier: db
spec:
  type: ClusterIp
  selector:
    project: roboshop
    component: mongodb
    tier: db
  ports:
    - protocol: TCP
      port: 27017 # service port
      targetPort: 27017  # container port

